import{describe,expect,it,vi}from"vitest";
import{DeterministicInstagramVisualAnalyzer}from"./instagram-visual-analysis";

describe("Instagram visual analysis",()=>{it("creates evidence-bound owner-reviewable directions from bounded raster samples",async()=>{const pixels=new Uint8Array(12).fill(224),raster={sample:vi.fn(async()=>({width:2,height:2,pixels}))},result=await new DeterministicInstagramVisualAnalyzer(raster).analyze([{id:"m1",thumbnailUrl:"https://cdn.example/m1.jpg"}]);expect(raster.sample).toHaveBeenCalledWith(expect.objectContaining({mediaId:"m1",maxBytes:5*1024*1024,maxPixels:2_000_000}));expect(result.map(item=>item.fieldKey)).toEqual(["color-direction","imagery-direction","typography-direction"]);expect(result.every(item=>item.evidenceMediaIds[0]==="m1")).toBe(true);expect(result[2]?.value).toContain("font cannot be identified reliably")});it("returns no visual claims when media cannot be sampled",async()=>{const result=await new DeterministicInstagramVisualAnalyzer({sample:async()=>null}).analyze([{id:"m1",mediaUrl:"https://cdn.example/m1.jpg"}]);expect(result).toEqual([])})});
