import{readFile}from"node:fs/promises";import{Pool}from"pg";import{afterAll,beforeAll,beforeEach,describe,expect,it}from"vitest";import{connectChannelAccount,createPublishCommand}from"@kairo/domain/publishing";import{createMetricSnapshot,normalizeMetricSnapshot}from"@kairo/domain/analytics";import type{ContentApproval}from"@kairo/domain/review";import{PgKairoRepository}from"./postgres-store";import{PgPublishingRepository}from"./publishing-postgres-store";import{PgPublishingExecutionStore}from"./publishing-execution-postgres-store";import{PgAnalyticsRepository}from"./analytics-postgres-store";
const url=process.env.TEST_DATABASE_URL;const suite=url?describe:describe.skip;
suite("PostgreSQL VS-07 publishing repository",()=>{const pool=new Pool({connectionString:url});const core=new PgKairoRepository(pool);const repo=new PgPublishingRepository(pool);beforeAll(async()=>{for(const[f,marker]of[["0001_identity_workspace_brand.sql","accounts"],["0002_brand_brain_knowledge.sql","knowledge_sources"],["0003_hunter_discover.sql","public_signals"],["0004_research_angles.sql","ideas"],["0005_campaign_content.sql","campaigns"],["0006_content_review_approval.sql","content_reviews"],["0007_channel_calendar_publishing.sql","publish_commands"],["0008_performance_tracking.sql","metric_snapshots"]]as const){const exists=await pool.query<{name:string|null}>("select to_regclass($1)::text name",[`public.${marker}`]);if(!exists.rows[0]?.name)await pool.query(await readFile(new URL(`../migrations/${f}`,import.meta.url),"utf8"))}});beforeEach(async()=>{await pool.query("truncate normalized_metrics,metric_snapshots,published_posts,publish_attempts,publish_commands,channel_accounts,content_approvals,content_reviews,content_versions,content_assets,campaigns,angles,claim_evidence,claims,evidence_references,research_dossiers,ideas,brand_opportunity_signals,brand_opportunities,public_signals,knowledge_source_derivations,brand_brain_field_sources,brand_brain_fields,knowledge_sources,audit_events,brands,workspace_memberships,workspaces,external_identities,accounts cascade")});afterAll(()=>pool.end());
it("atomically leases a version-bound publish and records published identity",async()=>{const user=await core.resolveAccount({provider:"test",subject:"alice"});const made=await core.createWorkspaceWithBrand(user.id,{workspaceName:"Studio",brandName:"Kairo"});const w=made.workspace.id,b=made.brand.id;const fixtures=[`insert into ideas(id,workspace_id,brand_id,title,premise,source_type,status,created_at) values('i',$1,$2,'Idea','Premise','user','research-ready',now())`,`insert into research_dossiers(id,workspace_id,brand_id,idea_id,summary,unresolved_uncertainties,status,created_at) values('r',$1,$2,'i','Research','[]','ready',now())`,`insert into angles(id,workspace_id,brand_id,idea_id,title,framing,audience,objective,hook_direction,expected_value,effort,recommended_format,recommended_channel,supporting_claim_ids,status,version) values('g',$1,$2,'i','Angle','Frame','Audience','Goal','Hook','Value','low','text','linkedin','[]','selected',1)`,`insert into campaigns(id,workspace_id,brand_id,idea_id,research_id,angle_id,name,objective,supporting_claim_ids,status,created_at) values('c',$1,$2,'i','r','g','Campaign','Goal','[]','draft',now())`,`insert into content_assets(id,workspace_id,brand_id,campaign_id,channel,format,audience,topic,hook_type,cta,supporting_claim_ids,current_version,status,created_at) values('a',$1,$2,'c','linkedin','text','Audience','Topic','fact','Read','[]',1,'draft',now())`,`insert into content_versions(id,workspace_id,brand_id,campaign_id,asset_id,version,content,supporting_claim_ids,actor,action,created_at) values('v',$1,$2,'c','a',1,'Hello','[]','user','manual-edit',now())`,`insert into content_reviews(id,workspace_id,brand_id,campaign_id,asset_id,version_id,version,status,truth,revision_cycle,requested_at,completed_at) values('q',$1,$2,'c','a','v',1,'passed','{}',0,now(),now())`];for(const sql of fixtures)await pool.query(sql,[w,b]);await pool.query(`insert into content_approvals(id,workspace_id,brand_id,campaign_id,asset_id,version_id,version,review_id,approver_account_id,destination,approved_at) values('p',$1,$2,'c','a','v',1,'q',$3,'{"channel":"linkedin","accountRef":"page"}',now())`,[w,b,user.id]);const ca=await repo.saveChannelAccount(user.id,connectChannelAccount({id:"ca",workspaceId:w,brandId:b,channel:"linkedin",accountRef:"page",displayName:"Kairo",credentialRef:"vault://ca",capabilities:["publish-text"],connectedAt:"2026-08-13T10:00:00Z"}));const approval:ContentApproval={id:"p",workspaceId:w,brandId:b,campaignId:"c",assetId:"a",versionId:"v",version:1,reviewId:"q",approverAccountId:user.id,destination:{channel:"linkedin",accountRef:"page"},approvedAt:"2026-08-13T10:00:00Z"};const command=createPublishCommand({id:"cmd",approval,currentVersionId:"v",channelAccount:ca,contentType:"text",scheduledFor:"2026-08-14T10:00:00Z",createdAt:"2026-08-13T10:00:00Z"});await repo.saveCommand(user.id,command);const execution=new PgPublishingExecutionStore(pool),job=await execution.claimNext("2026-08-14T10:00:00Z","worker-1",60);expect(job).toMatchObject({commandId:"cmd",content:"Hello",credentialRef:"vault://ca"});await execution.settle(job!,{status:"published",externalPostId:"urn:1"},"2026-08-14T10:01:00Z");expect((await repo.getCommand(user.id,b,"cmd"))?.status).toBe("published");const analytics=new PgAnalyticsRepository(pool),post=await analytics.getPublishedPost(user.id,b,(await pool.query("select id from published_posts")).rows[0].id);const snapshot=createMetricSnapshot({id:"snapshot-1",post:post!,provider:"linkedin",capturedAt:"2026-08-14T11:00:00Z",raw:{impressions:120}}),metrics=normalizeMetricSnapshot(snapshot,{version:"linkedin-v1",supported:{impressions:"impressions",shares:"shares"}});await analytics.append(user.id,snapshot,metrics);expect(await analytics.list(user.id,b)).toEqual(expect.arrayContaining([expect.objectContaining({name:"impressions",status:"available",value:120}),expect.objectContaining({name:"shares",status:"unavailable",reason:"provider-did-not-return"})]))})});
